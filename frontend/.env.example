# Base URL of the backend API.
# Leave empty to use the Vite dev proxy (/api -> http://localhost:8000).
# In production set this to your deployed backend, e.g. https://your-api.onrender.com
VITE_API_BASE_URL=
